<Page
    x:Class="Reversi.View.ScoreBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="using:Reversi.Model.classes"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock VerticalAlignment="Top" FontSize="48">対戦履歴</TextBlock>
        <ListView Name="ScoreList" Margin="0,70,0,0" ItemsSource="{x:Bind ScoreBoardPageViewModel.ScoreData}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="classes:ScoreData">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="52">プレイヤー：</TextBlock>
                        <TextBlock FontSize="52" Text="{x:Bind BlackScore, Mode=OneWay}" />
                        <TextBlock FontSize="52">　CPU：</TextBlock>
                        <TextBlock FontSize="52" Text="{x:Bind WhiteScore, Mode=OneWay}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>